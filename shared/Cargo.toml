[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
strum = { version = "0.27.2", features = ["strum_macros"] }
strum_macros = "0.27.2"

[dev-dependencies]
serde_json = "1.0"
